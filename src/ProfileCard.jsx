function ProfileCard({name, title, handle}){
    // const title = props.title;
    // const handle = props.handle
   
    return (
        <div>
            <div>title is {title} </div>
            <div>name is {name} </div>

            <div>Handle is {handle} </div>
        </div>
    )
}

export default ProfileCard